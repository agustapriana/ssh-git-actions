name: CI CD LAGI
on:
  push:
    branches: [ percobaan-1 ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Get Latest Code
        uses: actions/checkout@v2
      - name: Sync Files
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: 223.27.147.38
          username: researchlbs
          password: ${{ secrets.FTP_PASSWORD }}