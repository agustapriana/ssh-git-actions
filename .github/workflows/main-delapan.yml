name: CI CD LAGI
on:
  push:
    branches: [ percobaan-1 ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: sudo apt-get install sshpass
      - run: sshpass -p "LBSsuk4suk4" scp ./* researchlbs@limasakti.co.id:/home/researchlbs/public_html/apriana